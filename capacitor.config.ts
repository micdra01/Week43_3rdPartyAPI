import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Week43_3rdPartyAPI',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
